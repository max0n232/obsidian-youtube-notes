---
processed: true
category: "ai-tools"
source: "https://www.youtube.com/watch?v=kZhAj8--t8w"
date: 2026-04-12
method: whisper
---

## Краткое содержание


After testing the Gemma 4 model on my laptop and desktop, I decided to run a crazy experiment to try launching Gemma 4 on my Raspberry Pi 5. Of course, I'm not talking about any large versions of this model, but rather attempting to run the smallest one, E2B. I'm not sure if this will work, since even the smallest model may still require significant resources, but it's worth trying. My Raspberry Pi 5 is on my home network, so I always access it via SSH. I don't even have a graphical interface installed on it, just Ubuntu server, and a minimal set of software that I need. Hi, I'm Nick, and I've been developing software for over 20 years. On this channel, I share my experience, insights, and thoughts about IT. So I connect to my Raspberry Pi via SSH. I have the terminal multiplexer Tmux running there. It's a very convenient and useful tool if you often work in the terminal. Tmux allows you to keep sessions running even if you disconnect from another computer. Definitely give it a try if you're not familiar with it. Alright, first I launch Htop so you can see the specs of my Raspberry Pi. Just to remind you, this is version 5. As you can see, it has 4 cores and 8 gigabytes of RAM, most of which is currently free since I'm not running anything heavy at the moment. OK, the first thing I need to do is install LM Studio. However, since I don't need the graphical interface, I'll install only the CLI version. You could call it a headless version of LM Studio. The developers provide a special script that handles everything for you. After installation, it suggests starting the daemon right away. Alright, let's do that. Now let's check what commands are available. I see a set of commands for managing models, some for managing the local server, and a few others that I don't need right now. In general, the commands are pretty straightforward, but before launching anything, I want to change where the downloaded models will be stored. The thing is, I have an SSD connected to my Raspberry Pi, so I want the models to be stored there, instead of on the SD card. By the way, Raspberry Pi 5 introduced a simple way to connect SSDs and similar peripherals. I've been using such a drive for over a year now, and it's very convenient. Alright, the storage location is set. Now I'm downloading the smallest model from the Gemma 4 family. This model is called E2B, and it's about 4.5 gigabytes in size. While it's downloading, let me briefly explain what it can do according to Huul's announcement. First, the entire family of these models is designed with agent-based workflows in mind. They natively support function calling and can work with tools. Second, these models can handle images and video, and the smaller models, including the one I'm installing right now, also have native audio support, they can understand speech directly, and third, these models have a context window of 128,000 tokens, and support almost all languages. By the way, Gemma 4 is released under the fully open and commercially permissive Apache 2.0 license. That means you can do almost anything you want with these models. Alright, the model has finished downloading. As you can see, it has around 4 billion parameters, which should be enough for simple and smaller tasks. I'm loading the model now. OK, it loaded successfully and is ready to use. Now I'll launch Hotstop again to see what changed after loading the model. Looks like the model has been loaded into RAM. Alright, the last step is to start the API server so I can interact with the running model. I'll use port 4000. Alright, the server is running, but just to be safe, I'll check if it's working properly. To do that, I'll send a simple HTTP request to get the list of available LLM models. In that list, I expect to see the Gemma 4 model I just downloaded. Nice, the API request was successful and I got the list of models, and I can see Gemma 4 in that list. Perfect, everything is working. However, right now LLM Studio has started a local server that's only accessible on the Raspberry Pi itself. I want to use this model from my other computers over the local network, so I'll stop the server and restart it with an additional host parameter, setting it to 0.0.0.0, which should make it accessible on the local network. Hmm, that didn't work. It looks like the host parameter isn't available, and I can only specify the port when starting the server. But that's not a problem. Since this is Linux, we have all its power and hundreds of utilities at our disposal. So there are plenty of ways to solve this. I'll go with the fastest and simplest solution, using the Soshat utility. It allows you to forward network streams within the system, which is exactly what I need. I'll forward the internal port 4000, where the LLM Studio server is running, to an external interface on port 4001. This effectively creates a bridge between the internal and external ports. Now any request coming to the Raspberry Pi on port 4001 will be redirected internally to LLM Studio. Alright, now I'll disconnect from the SSH session on the Raspberry Pi, and so to speak return to my Macbook. Now I'm in the regular macOS terminal, I'll try sending the same HTTP request as before to get the list of available models, but this time not to localhost, but to my Raspberry Pi, which as a reminder is on the same local network as my laptop. Nice, it worked. As you can see, I received a response from the LLM Studio server running on my Raspberry Pi, and in that response I can see Gemma 4. Now everything is finally set up the way I wanted, and we can move on to the most interesting part, actually testing Gemma 4 on the Raspberry Pi. Since the local server started by LLM Studio fully mimics the OpenAI API, it means that any application where you can specify a custom OpenAI endpoint or base URL can work with your local model. This could be chat interfaces like Chatbox AI or OpenWebUI, or your editors and IDEs, for example VS Code Cursor or Zed Editor, I'll be using the last one today. Alright I open the settings in the Zed Editor and add a new entry in the LLM section for the Gemma 4 model running on my Raspberry Pi. Since I added a similar entry a couple of days ago for my desktop, I'll just copy it and update the server URL and the model name. By the way, I have a separate video about running and testing Gemma 4 on my Macbook and desktop. Now if we go to the settings and open the LLM Provider section, we should see an entry corresponding to our Raspberry Pi, and here it is. Ok now in the chat at the bottom I'll select the Gemma 4 model running on the Raspberry Pi so that it's used in this session. Let's send a simple prompt to check if everything is set up correctly, and whether our local network setup works inside the editor. Alright the model started thinking, that's a good sign, but just to be sure, let's wait for the response. Nice, the model replied. Let's take a look at the reasoning section. Looks pretty standard. The usual thinking process you'd expect from these models. So what do we have at this point? First Gemma 4 is successfully running on the Raspberry Pi, even if it's the smallest version. Second, I'm able to interact with it from another computer over the local network. I think it's time to push this model to its limits. To do that, I'll connect to the Raspberry Pi via terminal and start monitoring system resources, so we can see how much load the response generation puts on it. Then I'll give the model my standard test task, writing a Python function to sort a list of objects. Alright the model started thinking. As you can see the Raspberry Pi is fully loaded, all CPU cores are in use. Now the model has finished reasoning and started generating the response. I'll show this part in real time, without speeding it up so you can get a sense of the actual generation speed. Overall, the speed is fairly acceptable for simple interactions or scripts, where response time isn't critical. Now to save time, I'll speed up the rest of the generation, because producing the full response on a Raspberry Pi can take quite a while. At the end, I'll show the total time it took. Alright the full response is ready. Not only did the model write a sorting function, but it actually provided two different implementations and explained when to use each. Pretty impressive. However, the total time, including the reasoning phase, was about 6 minutes. I think this could be reduced a bit by disabling the reasoning mode, but overall you get the idea of the performance. Now for completeness, let's try a non-programming task. I'll ask the model to come up with three web app ideas. After a short thinking phase, which I've sped up, the model starts generating ideas. What you're seeing now is the real generation speed. From my perspective, it's about the same as in the previous task. I'd say this speed is right on the edge of what feels comfortable for a human to read in real time, but for non-interactive scenarios, it's acceptable. I'll speed up the rest again and check the total time. Alright this time, Gemma took about 5 minutes to generate the full response, but it produced a lot of text and described each idea in detail. In my opinion, that's a very solid result for such a small model, especially running on a Raspberry Pi. So now you have the full picture of running Gemma 4 on a Raspberry Pi 5. As you can see, it's absolutely possible, and the results are usable, at least for certain scripts or automation tasks. This setup can definitely make sense. Alright that's it for today. If you enjoyed this video, don't forget to like and subscribe so you don't miss the next one. See you soon. 
